{{- if and .Values.opentelemetry.enabled .Values.opentelemetry.collector.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "strato-control-plane.fullname" . }}-otel-collector
  labels:
    {{- include "strato-control-plane.labels" . | nindent 4 }}
    app.kubernetes.io/component: otel-collector
  {{- $ann := include "strato-control-plane.scrapeAnnotations" (dict "ctx" $ "port" .Values.opentelemetry.collector.service.prometheusPort) }}
  {{- if and $ann (include "strato-control-plane.metricsExposed" .) }}
  annotations:
    {{- $ann | nindent 4 }}
  {{- end }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.opentelemetry.collector.service.grpcPort }}
      targetPort: grpc
      protocol: TCP
      name: grpc
    - port: {{ .Values.opentelemetry.collector.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.opentelemetry.collector.service.metricsPort }}
      targetPort: metrics
      protocol: TCP
      name: metrics
    {{- if include "strato-control-plane.metricsExposed" . }}
    - port: {{ .Values.opentelemetry.collector.service.prometheusPort }}
      targetPort: prometheus
      protocol: TCP
      name: prometheus
    {{- end }}
  selector:
    {{- include "strato-control-plane.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: otel-collector
{{- end }}
