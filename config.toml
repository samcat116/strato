# Strato Agent Configuration

# WebSocket URL for connecting to Control Plane (with registration token)
control_plane_url = "ws://localhost:8080/agent/ws?token=FINAL-TEST-TOKEN-2025&name=hypervisor-1"

# QEMU socket directory (default: /var/run/qemu)
qemu_socket_dir = "/var/run/qemu"

# Logging level (default: info)
log_level = "debug"
