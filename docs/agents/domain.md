# Domain documentation

For domain terminology, use the vocabulary in `CONTEXT.md` at the repository root.
For a design decision, consult the relevant ADR in `docs/adr/` and identify any
conflict with it explicitly, including why the decision should be reopened.

When a requested change resolves a new term or changes an architectural
decision, update the relevant glossary entry or ADR in the same change.
